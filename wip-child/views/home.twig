{% extends "base.twig" %}

{% block bannerinner %}
    {% if banners_top %}
        {% for banner in banners_top %}
            {% include ['partials/banner-'~post.post_type~'.twig', 'partials/banner.twig'] %}
        {% endfor %}
    {% else %}
        {% do action('mwh_banner') %}
    {% endif %}
{% endblock %}


{% block content %}

    <div class="loop loop--cards loop--posts">
        {% for post in posts %}
            {% do action('tha_entry_before') %}
            {% include ['partials/entry-'~post.post_type~'.twig', 'entry-post.twig'] %}
            {% do action('tha_entry_after') %}
        {% endfor %}
    </div>

{% endblock %}