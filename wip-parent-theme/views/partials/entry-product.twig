<article {{ fn('post_class', ['$classes', 'entry'] ) }}>

    <div class="Media">

        {% do action('tha_entry_top') %}

        <div class="Media-figure">
            <a href="{{entry.post.link}}">
                {% if entry.post.thumbnail %}
                    <img src="{{ entry.post.thumbnail.src|resize(300, 225) }}" />
                {% else %}
                    <span class="thumb-placeholder"><i class="icon-camera"></i></span>
                {% endif %}
            </a>
        </div>

        <div class="Media-content">
            {% do action('woocommerce_before_shop_loop_item_title') %}
            <!-- {% if entry.product.is_on_sale %}SALE!{% endif %} -->
            <h2 class="entry-title"><a href="{{entry.post.link}}">{{entry.post.title}}</a></h2>
            <!-- <p class="price">{{entry.product.get_price_html}}</p> -->
            {% do action( 'woocommerce_after_shop_loop_item_title' ) %}

            <a href="{{item.product.add_to_cart_url}}" class="button">
                Add to Cart
            </a>
        </div>

        {% do action('tha_entry_bottom') %}

    </div>

</article>

<!--

<li>
    <div class="product">
    <a href="{{item.post.permalink}}">
                    <img class="productimage" src="{{item.post.get_thumbnail.get_src|resize(300, 300)}}" />
                </a>
    <a href="{{item.post.permalink}}">
         {{ item.post.title }}
    </a>
    <p class="price">{{item.product.get_price_html}}</p>
    <a href="{{item.product.add_to_cart_url}}" class="button">
        Add to Cart
    </a>
    </div>
</li>
-->