{% do action('tha_entry_before') %}

<article {{ fn('post_class', ['$classes', 'entry'] ) }}>

    {{ fn('timber_set_product', post)}}

    <div class="Media">

        {% do action('tha_entry_top') %}

        {% if post.thumbnail and showthumb %}
            <div class="Media-figure">
                <a href="{{post.link}}">
                    {% if post.thumbnail %}
                        <img src="{{ post.thumbnail.src|resize(300, 225) }}" />
                    {% else %}
                        teste
                        <span class="thumb-placeholder"><i class="icon-camera"></i></span>
                    {% endif %}
                </a>
            </div>
        {% endif %}

        <div class="Media-content">
            {% do action('woocommerce_before_shop_loop_item_title') %}
            {% if post.title %}
                <h2 class="post-title"><a href="{{post.link}}">{{ post.title }}</a></h2>
            {% else %}
                <h2 class="post-title"><a href="{{post.link}}">{{ fn('the_title') }}</a></h2>
            {% endif %}
            {% do action( 'woocommerce_after_shop_loop_item_title' ) %}
            {% do action( 'woocommerce_after_shop_loop_item' ) %}
        </div>

        {% do action('tha_entry_bottom') %}

    </div>

</article>

{% do action('tha_entry_after') %}